
export const posts = (state = [], action) => {
    switch (action.type) {
      
      default:
        return state
    }
}



export const postsAreLoading = (state = false, action) => {
    switch (action.type) {
      
      default:
        return state
    }
}

